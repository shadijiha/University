#include "__PATH__/core.shado"
//#include "__PATH__/main.shado"

int min(int a, int b)   {
    if (a < b)  {
        return a;
    } else  {
        return b;
    }
}

int main()	{
    int[] array = {1, 13, 84, 1, 3};
    int a = min(5, 6);

    print(a);

}